{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "FastAPI",
            "type": "debugpy",
            "request": "launch",
            "module": "uvicorn",
            "args": [
                "main:app",
                "--reload"
            ],
            "jinja": true,
            "python": "${workspaceFolder}/venv/bin/python"  
        },
        {
            "name": "zmq_worker",
            "type": "python",
            "request": "launch",
            "module": "comms.zmq_worker",              // ✅ Run as module
            "cwd": "${workspaceFolder}",               // ✅ Set working directory to project root
            "justMyCode": true
            }
    ]
}
